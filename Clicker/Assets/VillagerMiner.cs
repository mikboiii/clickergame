using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VillagerMiner : MonoBehaviour
{
    //use if miners are actual entities
    int resourcesCarried;
    /* Stuff to do
     * Function to add resources to total
     * Function to change animation states
     */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
